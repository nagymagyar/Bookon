import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-note',
  imports: [RouterLink],
  templateUrl: './note.html',
  styleUrl: './note.css',
})
export class Note {

}
